import Button from "@mui/material/Button";

const StartGame = () => {
  return (
    <Button variant="contained" size="large" color="primary" style={{margin: '0', width: '300px', height: '90px', marginTop: '70px', fontSize: '2rem'}}>
      Start Game
    </Button>
  );
};

export { StartGame };
