import Button from "@mui/material/Button";

const StartGame = () => {
  return (
    <Button variant="contained" size="large" color="primary">
      Start Game
    </Button>
  );
};

export { StartGame };
