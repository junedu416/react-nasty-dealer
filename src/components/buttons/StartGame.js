import Button from "@mui/material/Button";

const StartGame = () => {
  return (
    <Button variant="contained" size="large" color="primary" style={{margin: '0', boxSizing: 'border-box',}}>
      Start Game
    </Button>
  );
};

export { StartGame };
